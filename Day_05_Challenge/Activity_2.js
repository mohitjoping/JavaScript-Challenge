// Activity 2: Function Declaration     
// Task 3: Write a function expression to find the maximum of two numvers and log the result to the console.

let max = function(a, b) {
    if (a > b) {
        console.log(a);
    } else {
        console.log(b);
    }
}

// Task 4: Write a function expression to concatenate two strings and return the result.

let concat = function(str1, str2) {
    return str1 + str2;
}   
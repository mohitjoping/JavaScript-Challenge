// Activity 1: If-Else Statements
// Task 1: Write a Program to check if a number is positive, negative, or zero, and log the result to the console.
    let a = 0
    if (a>0) {
        console.log(`${a} : number is positve`);
    } else if (a<0) {
        console.log(`${a} : number is negative`);
    } else {
        console.log(`${a} : number is zero`);
    } 

// Task 2: Write a program to check if a person is eligible to vote (age >= 18) and log the result to the console.
    let age = 17
    
    if (age>=18) {
        console.log('This person meets the eligible criteria');
    } else {
        console.log("This person doesn't meets the eligible criteria")
    }
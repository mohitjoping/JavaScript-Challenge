// Activity 4: Logical Operators
// Task 11: Write a program that uses the && operator to combine two conditions and log the result to the console.
    let a = 10
    let b = 10
    let c = 10

    console.log(a==b && b==c);
    c = 20
    console.log(a==b && b==c);
// Task 12: Write a program that uses the || operator to combine two conditions adn log the result to the console.

    console.log(a==b || b==c);

    b = 20
    c = 30

    console.log(a==b || b==c);

// Task 13: Write a program that uses the ! operator to negate a conditions adn log the result to the console.

    let isLoggedIn = false;

 
    let notLoggedIn = !isLoggedIn;

    console.log("Logged In:", isLoggedIn);
    console.log("Not Logged In:", notLoggedIn);
